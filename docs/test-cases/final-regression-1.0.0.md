# Финальное регрессионное тестирование FleetControl 1.0.0

## Общая информация

Проверяемая ветка: `develop`

Цель тестирования — убедиться, что интегрированная версия FleetControl
готова к формированию release candidate 1.0.0 и ранее реализованные
сценарии продолжают работать корректно.

## Автоматизированное тестирование

### RT-001 — Полный набор автоматизированных тестов

Команда:

`npm test`

Ожидаемый результат:
все test suites и tests завершены успешно.

Фактический результат:

Статус: PASS

---

## Контейнерное развёртывание

### RT-002 — Сборка и запуск Docker Compose

Ожидаемый результат:
сервисы `app` и `postgres` успешно запускаются,
PostgreSQL находится в состоянии healthy.

Фактический результат:

Статус: PASS

### RT-003 — Health endpoint

Ожидаемый результат:
`GET /health` возвращает HTTP 200.

Фактический результат:

Статус: PASS

### RT-004 — Получение автомобилей

Ожидаемый результат:
`GET /vehicles` возвращает HTTP 200 и список автомобилей.

Фактический результат:

Статус: PASS

### RT-005 — Получение водителей

Ожидаемый результат:
`GET /drivers` возвращает HTTP 200 и список водителей.

Фактический результат:

Статус: PASS

---

## Основной бизнес-сценарий

### RT-006 — Назначение подменного автомобиля

Ожидаемый результат:
активному водителю назначается доступный автомобиль;
HTTP 201; автомобиль переходит в `IN_USE`.

Фактический результат:

Статус: PASS

### RT-007 — Возврат подменного автомобиля

Ожидаемый результат:
назначение завершается;
`ended_at` заполняется;
автомобиль возвращается в `AVAILABLE`;
HTTP 200.

Фактический результат:

Статус: PASS

---

## Регрессия исправленных дефектов

### RT-008 — Некорректный идентификатор

Ожидаемый результат:
некорректный `driver_id` или `vehicle_id` возвращает HTTP 400,
а не HTTP 500.

Фактический результат:

Статус: PASS

### RT-009 — Неактивный водитель

Ожидаемый результат:
водителю со статусом `INACTIVE` нельзя назначить автомобиль;
HTTP 409.

Фактический результат:

Статус: PASS

### RT-010 — Повторный возврат автомобиля

Ожидаемый результат:
повторное завершение уже закрытого назначения возвращает HTTP 409.

Фактический результат:

Статус: PASS

---

## Сохранность данных

### RT-011 — Persistent volume PostgreSQL

Ожидаемый результат:
после `docker compose down` и повторного запуска данные БД сохраняются.

Фактический результат:

Статус: PASS

---

## Итог

Пройдено: 11 / 11

Обнаружено дефектов: 0

Заключение: PASS